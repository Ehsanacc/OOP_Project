package Model;

public class AI extends User{
    public AI(String userName, String password, String nickname, String email, String question, String answer) {
        super(userName, password, nickname, email, question, answer);
    }
}
